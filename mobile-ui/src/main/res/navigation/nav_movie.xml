<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_movie"
    app:startDestination="@+id/navigation_home_movie">

    <fragment
        android:id="@+id/navigation_home_movie"
        android:name="com.example.mymallupgrade.ui.movie.popular.MovieFragment"
        android:label="Home"
        tools:layout="@layout/fragment_movie">
    </fragment>

    <fragment
        android:id="@+id/navigation_favorite_movie"
        android:name="com.example.mymallupgrade.ui.movie.favorite.FavoriteMovieFragment"
        android:label="Favorite"
        tools:layout="@layout/fragment_favorite_movie">
    </fragment>

    <fragment
        android:id="@+id/navigation_account"
        android:name="com.example.mymallupgrade.ui.home.ui.send.SendFragment"
        android:label="Account"
        tools:layout="@layout/fragment_send" />
</navigation>